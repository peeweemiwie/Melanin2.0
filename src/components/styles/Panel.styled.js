import styled from 'styled-components';

export const PanelStyled = styled.div`
	background-color: var(--gray0);
	grid-area: panel;
	padding: var(--space1);
	padding-top: 6rem;
	img {
		max-width: 100%;
	}
`;
